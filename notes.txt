--To create an environment using conda command-- conda create -p venv python==3.10
--To activate the environment using conda command-- conda activate venv/
--To deactivate the environment using conda command-- conda deactivate
--For ipynb files running codes in shell mode, we need to install ipykernel packagee -- pip install ipykernel
-- To install all the packages written in requirement.txt -- pip install -r /path/to/requirements.txt